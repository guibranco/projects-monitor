# Scripts

## fetch_appveyor_builds.php

This script is responsible for fetching build information from AppVeyor for repositories that contain an `appveyor.yml` file.

### Usage

1. Ensure you have set up your database connection and AppVeyor API token in the script.
2. Run the script using PHP: `php fetch_appveyor_builds.php`
